"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CrossForkLogger = void 0;
class CrossForkLogger {
    constructor() { }
    debug(msg) {
        this.sendLog('debug', msg);
    }
    info(msg) {
        this.sendLog('info', msg);
    }
    warn(msg, title = '') {
        this.sendLog('warn', msg, title);
    }
    error(msg, title = '') {
        this.sendLog('error', msg, title);
    }
    log(msg, level) {
        this.sendLog('log', msg, level);
    }
    sendLog(level, ...args) {
        process.send &&
            process.send({
                crossForkLogMessage: {
                    level,
                    args: args,
                },
            });
    }
}
exports.CrossForkLogger = CrossForkLogger;
