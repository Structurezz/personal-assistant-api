import { LoggerInstance } from '../types';
type Options = {
    shouldPrintMessage: boolean;
    shouldThrowError: boolean;
    functionName?: string;
    logger?: LoggerInstance;
};
export declare function checkForValidAuthToken(authToken: string | undefined, options?: Options): boolean;
export {};
