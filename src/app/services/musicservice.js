// Mock service - replace with actual implementation
export const playMusic = async (song) => {
    // Here you might integrate with a music API or service
    return { message: `Playing song: ${song}` };
};
